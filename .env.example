# Supabase credentials (publishable/anon only - never commit service keys)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional Google Analytics 4 measurement ID (e.g. G-XXXXXXX). Analytics only loads
# after a visitor accepts the cookie consent banner. Leave empty to disable.
# VITE_GA_MEASUREMENT_ID=